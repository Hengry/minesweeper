## Quick start:

```js
yarn && yarn start

// or npm
npm install && npm run start
```

## Step by step

First, install dependenciesL

```js
yarn

// or npm
npm install
```

Then start the project:

```js
yarn start

// or npm
npm run start
```
